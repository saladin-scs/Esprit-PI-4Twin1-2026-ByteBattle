// Integration example kept intentionally non-executable so it does not affect builds.
// Add the ML routes in [frontend/src/core/routes/AppRoutes.tsx](src/core/routes/AppRoutes.tsx) and
// use [frontend/src/modules/ml/MLRoutes.tsx](src/modules/ml/MLRoutes.tsx) as the route group.

export {};
