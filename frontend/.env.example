# Copier vers .env — doit être identique à l’URL d’écoute du backend (PORT, ex. 3000).
# Backend : CORS_ORIGIN doit inclure l’URL du front Vite (défaut http://localhost:5173 et 127.0.0.1:5173).
VITE_API_URL=http://localhost:3000
